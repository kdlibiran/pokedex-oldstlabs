            <p className="flex w-[50vw] flex-col">
              Moves:{" "}
              {info.moves &&
                info.moves
                  .map(
                    (move) =>
                      move.move.name[0].toUpperCase() + move.move.name.slice(1),
                  )
                  .join(", ")}
            </p>
